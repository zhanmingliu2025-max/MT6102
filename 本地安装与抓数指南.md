# 方案二：本地 Claude Code + 浏览器控制 —— 安装与抓数指南

目标：在你自己的电脑上让 Claude 操控浏览器，登录态留在本地，批量抓取 Clarksons SIN 数据并自动留痕。

---

## 第 1 步：安装 Claude Code CLI

**前置**：Node.js 18+（https://nodejs.org 下载 LTS 版即可）。

```bash
# Mac / Windows PowerShell 通用
npm install -g @anthropic-ai/claude-code

# 验证
claude --version
```

首次运行 `claude` 会引导你登录 Claude 账号（浏览器授权，跟网页版同一个账号）。

## 第 2 步：克隆本仓库并进入

```bash
git clone https://github.com/zhanmingliu2025-max/MT6102.git
cd MT6102
git checkout claude/confident-rubin-lpa9y8
```

仓库里已备好：
- `SIN_数据采集任务书.md` —— 本地 Claude 的执行清单（核心文件）
- `MT6102_Group_Project_投资方案.md` —— 整体方案，说明每项数据的用途

## 第 3 步：给本地 Claude 装上浏览器控制（Playwright MCP）

在仓库目录里执行：

```bash
claude mcp add playwright -- npx @playwright/mcp@latest
```

两种连接浏览器的方式，选其一：

### 方式 A（推荐，最简单）：独立受控浏览器 + 手动登录一次
什么都不用再配。启动 `claude` 后第一次让它开网页时，会弹出一个受 Claude 控制的 Chrome 窗口——**你在这个窗口里手动走 NTU 图书馆入口登录 SIN**（含 MFA），登录完成后告诉 Claude "已登录，继续"。该窗口使用持久化 profile，之后的会话通常还保留登录态。

### 方式 B：接管你日常在用的 Chrome
如果 SIN 登录绑定了你日常 Chrome 的某些插件/证书：

```bash
claude mcp remove playwright
claude mcp add playwright -- npx @playwright/mcp@latest --extension
```

然后按提示在 Chrome 安装 "Playwright MCP Bridge" 扩展，Claude 即可操作你当前浏览器中已登录 SIN 的标签页。

## 第 4 步：开始抓数

在仓库目录运行 `claude`，然后输入：

> 请阅读 SIN_数据采集任务书.md，按任务书要求逐项抓取数据。每完成一项就更新 data/catalog.csv 并保存截图。先从 P0 优先级开始。

Claude 会：打开 SIN → 按任务书导航到指定模块 → 抓取序列/成交记录 → 存为 `data/raw/` 下的 CSV → 截图存 `data/screenshots/` → 在 `data/catalog.csv` 登记（指标 ID、序列全名、提取日期、区间、文件名）。

**中途需要你做的**：只有登录/MFA，以及偶尔确认页面弹窗。

## 第 5 步：抓完回传给我

```bash
git add data/ && git commit -m "Add SIN data batch 1" && git push
```

推送后回到网页版会话告诉我一声，我拉取数据后负责：清洗 → 画图 → 收益模型 → slide 文案。

---

## 安全须知
- NTU 账号密码只在你本地浏览器输入，**不要**写进任何提示词、文件或对话。
- 抓数仅限你有合法访问权的 SIN 账号，数据仅用于课程作业（SIN 许可范围内的学术用途）。
- 如果 Claude 请求做登录之外的敏感操作（改密码、发邮件等），拒绝即可——任务书里没有这类步骤。

## 常见问题
| 问题 | 处理 |
|---|---|
| `claude: command not found` | 重开终端；Windows 检查 npm 全局路径是否在 PATH |
| 浏览器窗口没弹出 | 对 Claude 说"用 playwright 打开 https://sin.clarksons.net" |
| SIN 登录态丢失 | 在受控窗口重新登录一次即可，profile 会记住 |
| 导出按钮点不动 | 让 Claude 改为"读取页面表格数据并整理成 CSV"（任务书已写明此备选路径） |
